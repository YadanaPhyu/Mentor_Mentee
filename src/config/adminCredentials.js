// Admin credentials - in a real app, this would be handled securely on the backend
export const ADMIN_CREDENTIALS = {
  email: 'admin@example.com',
  // This is a demo password, in production this would be handled by the backend
  password: 'admin123' // In production, NEVER store plain text passwords
};